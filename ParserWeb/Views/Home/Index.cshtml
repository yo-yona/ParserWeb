@using ParserWeb.Models
@model IEnumerable<SiteContent>
@{
    ViewData["Title"] = "Home Page";
}

<form asp-controller="Home" asp-action="Result" method="get">
    <p>
        Site URL: <input type="text" name="site" />
        <input type="submit" value="Print statistics" />
    </p>
</form>

<table class="table">
    <tr><td>Date</td><td>Site</td><td>Count</td><td>Word</td></tr>
    @foreach (SiteContent c in Model)
    {
            <tr><td>@c.Site.Date</td><td>@c.Site.Site</td><td>@c.Count</td><td>@c.Word</td></tr>
    }
</table>
